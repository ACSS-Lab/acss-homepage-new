---
// Neutral pill for short labels (application areas, tags).
---

<span class="chip"><slot /></span>

<style>
  .chip {
    padding: 5px 12px;
    border-radius: var(--radius-full);
    background: var(--gray-100);
    font: 500 var(--fs-caption) / 1.4 var(--font-sans);
    color: var(--gray-700);
  }
</style>
